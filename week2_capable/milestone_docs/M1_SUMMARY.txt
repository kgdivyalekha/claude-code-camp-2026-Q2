================================================================================
M1 — Event Store + Analytics + Token Baseline ✅ COMPLETE
================================================================================

DELIVERABLES IMPLEMENTED:

1. EventStore (src/boukensha/observability/event_store.py)
   ✓ Subscribes to Logger.subscribe()
   ✓ Writes events to events.db with WAL + mmap pragmas (from M0)
   ✓ DB faults degrade to warnings (never interrupt turns)
   ✓ rebuild_from_jsonl() can backfill from existing JSONL sessions

2. Analytics (src/boukensha/observability/analytics.py)
   ✓ token_breakdown() — Estimate schema/history/results (THE M1 criterion)
   ✓ cost_summary() — Total cost, per-turn breakdown
   ✓ tokens_per_turn() — Per-turn token accounting
   ✓ schema_overhead() — Validate §3.2 estimates (~30% of input)
   ✓ cache_effectiveness() — Measure cache hit rates
   ✓ iterations_per_turn() — Identify quadratic cost drivers
   ✓ tool_usage() — Tool call counts and success rates
   ✓ context_pressure() — Input tokens vs. context window

3. Schema (events.db)
   ✓ CREATE TABLE events with all fields for token accounting
   ✓ Separate cache_read/cache_write tokens (90% discount vs 25% premium)
   ✓ tools_sent column (enables §3.2 measurement)
   ✓ Indexes on (session_id, phase), (session_id, turn), (session_id, actor)

4. Measurement Tools
   ✓ measure_baseline.py — Run on any JSONL to get token report
   ✓ verify_m1.py — Standalone verification script
   ✓ Test fixture: test/fixtures/sessions/baseline_fixture.jsonl

5. Testing
   ✓ test/test_event_store.py — Unit tests for store operations
   ✓ test/test_m1_baseline.py — Integration test with fixture JSONL

ARCHITECTURE DECISIONS IMPLEMENTED:

✓ JSONL canonical; events.db is derived cache (can rebuild anytime)
✓ No observability fault may end a turn (all errors log + continue)
✓ Token accounting: usage ONLY on "response" events
✓ Cache tokens tracked separately (essential for measuring savings)
✓ Iteration tracking (cost scales ~N×context_size)

HOW TO USE:

1. Measure baseline on a real week1 session:
   python3 measure_baseline.py .boukensha/sessions/session.jsonl

2. Run tests:
   pytest test/test_event_store.py -v
   pytest test/test_m1_baseline.py -v

3. Verify implementation:
   python3 verify_m1.py

SUCCESS CRITERIA (§11 — M1):

✅ events.db grows live during a run (EventStore.attach works)
✅ rebuild_from_jsonl reproduces identical results (backfill works)
✅ token_breakdown() runs on fixture without error (M1 criterion met)
✅ Analytics queries work on all test data
✅ Fixture JSONL provides realistic test data with multiple turns/iterations

READY FOR M2:

M1 is the foundation for measuring all optimizations. M2 will build the log_viz
/tokens dashboard to visualize this baseline and enable before/after comparison
for M3-M8 optimizations.

When the dashboard (M2) is live:
- Every turn will be visible with token breakdown
- Each optimization can be measured against M1 baseline
- Hypothesis from §1.1 will be confirmed/corrected by real data

FILES CREATED:

src/boukensha/observability/
├── __init__.py (exports EventStore, Analytics, TokenBreakdown, CostSummary)
├── event_store.py (6.6 KB)
└── analytics.py (13.4 KB)

test/
├── test_event_store.py (8.7 KB)
├── test_m1_baseline.py (8.9 KB)
└── fixtures/sessions/baseline_fixture.jsonl (4.3 KB)

Root:
├── measure_baseline.py (5.9 KB)
├── verify_m1.py (5.8 KB)
├── M1_BASELINE.md (documentation)
└── M1_SUMMARY.txt (this file)

NEXT: M2 — log_viz /tokens dashboard
================================================================================
